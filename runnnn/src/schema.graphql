type Artifact  {
    id:String!
    title: String!
    maxPass: number!
    destination:String!
  }
  type Query {
    artifacts: [Artifact!]!
    ping:String!
  }

  type Mutation {
    deleteRunway:Artifact
  }
  